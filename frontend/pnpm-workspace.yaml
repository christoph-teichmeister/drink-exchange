# No dependency currently needs an install/postinstall script (Vite 8 ships
# prebuilt rolldown/lightningcss binaries). Add packages here only when pnpm
# reports ignored build scripts that are actually required.
onlyBuiltDependencies: []
